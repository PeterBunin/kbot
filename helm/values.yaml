# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
autoscaling:
  enabled: true
#ingress:
#  enabled: false
image:
  registry: ghcr.io
  repository: peterbunin/kbot
  tag: "1.0.0-33c92c2"
  arch: amd64
  os: linux
secret:
  name: kbot
  key: token
  env: TELE_TOKEN
securityContext:
  privileged: true
