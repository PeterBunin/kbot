# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
autoscaling:
  enabled: true
#ingress:
#  enabled: false
image:
  repository: letsmakesome/kbot
  tag: "v1.0.0-9894f3b"
  arch: amd64
secret:
  name: kbot
  key: token
  env: TELE_TOKEN
securityContext:
  privileged: true
